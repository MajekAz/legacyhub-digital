# Locally hosted brand fonts

- Inter variable, weights 400–700, Latin and Latin Extended subsets
- Source Serif 4 variable, weights 400–600, Latin and Latin Extended subsets

The files were downloaded from the official Google Fonts static host and are served locally so page rendering does not depend on a third-party font request. The relevant SIL Open Font License text is included alongside each family.
